# Starship Module

This module configures the Starship prompt.

## Maintenance

The `plain-text-symbols.toml` file is generated using the starship preset command. If you need to update it (e.g. after a starship update), run:

```bash
starship preset plain-text-symbols > ~/nix-config/modules/starship/plain-text-symbols.toml
```
